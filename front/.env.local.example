# Backend API (used by next.config rewrite + sidebar API docs link)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: monitoring stack URLs (only set in local dev — leave blank in production)
NEXT_PUBLIC_MLFLOW_URL=http://localhost:5500
NEXT_PUBLIC_GRAFANA_URL=http://localhost:3100
